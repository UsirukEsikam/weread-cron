> *This was generated by AI during triage.*

# 20: Docker deployment and operations alignment

**Findings:**
- F1 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
- F2 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
- F3 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
- F4 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
- F6 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
- F8 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md

**Category:** enhancement（含 F3 的 Git 忽略规则安全项）
**Blocked by:** None
**Status:** ready-for-agent

**What to build:** 部署/运维对齐：默认本地部署改为 `compose.yaml.example` 模板 → 用户复制为被 Git 忽略的 `compose.yaml`；`/data` 默认改 Docker named volume；`.gitignore` 覆盖 `compose.yaml` 与 `data/`；默认部署文档与 Compose 镜像路径指向本仓库发布的 `ghcr.io/UsirukEsikam/weread-cron`（fork 发布另述）；README 补齐 `books` 与镜像更新的 Compose 操作示例；README 明确损坏 persisted Login Session 的人工恢复边界。

## Current behavior

- F1: 仓库根提交 `docker-compose.yml` 作为活动部署文件，README 直接引用；无 `compose.yaml.example` 模板。
- F2: compose 示例以 `./data:/data` 绑定宿主仓库本地目录；`/data` 存 Login Session 与 Terminal State（应用内部状态）。
- F3: `.gitignore` 只覆盖 `.env` 等秘密文件，无 `/compose.yaml`、无 `/data/`；`data/` 内含完整 persisted Cookie 集合，存在误入库泄露风险。
- F4: README 与 compose 使用 `ghcr.io/OWNER/weread-cron`、要求普通用户替换 OWNER 为**自己的** GitHub 用户名；release.yml 实际推 `ghcr.io/${{ github.repository }}`。clone 用户在自己的 GHCR 命名空间并没有这个镜像。
- F6: README 只有 `run` 的 compose 调用示例；`books` 没有对应示例；没有镜像更新（pull + recreate）流程；文档需与新部署模型保持一致。
- F8: `session.New` 在持久化 Login Session 存在但解码失败/为空时启动报错、不回退初始 Cookie（保守失败，V1 决定）；README「更新 Cookie 后重启自愈」仅覆盖登录失效通知路径，未说明损坏会话文件的恢复方式。

## Desired behavior

1. **F1** 仓库提交 `compose.yaml.example`（部署模板）;用户复制为 `compose.yaml` 并修改本地部署值;`docker compose` 直接使用 `compose.yaml`（compose 默认文件名）。`compose.yaml` 是本地部署状态，不入 Git。已提交的 `docker-compose.yml` 不再作为活动部署文件（删除或明确废弃，避免两份示例并存）。
2. **F2** 默认 compose 用 Docker named volume 持久化 `/data`（应用内部状态，非用户工作目录）。开发/定制部署仍可使用本地 `data/` 目录，但须保持不入 Git。
3. **F3** `.gitignore` 增加 `/compose.yaml` 与 `/data/`；既有 `.env` 等秘密文件规则保持不变。
4. **F4** 默认部署文档与 compose 示例的镜像路径 = 本仓库发布的 `ghcr.io/UsirukEsikam/weread-cron`;不再指示普通用户替换 OWNER 为自己的用户名。fork 后自行发布作为单独的可选场景说明。
5. **F6** README 补齐:（a）`books` 的 compose 调用示例（如 `docker compose exec weread-cron /weread-cron books`）;（b）完整镜像更新流程（`docker compose pull` + `docker compose up -d`，或 recreate）;（c）与新 `compose.yaml.example` → `compose.yaml` 模型一致。
6. **F8** README 明确损坏 persisted Login Session（`login_session.json` 存在但解码失败/为空）的人工恢复边界:启动报「恢复 Login Session 失败」时，人工删除损坏文件（或恢复备份）后重启，容器以初始 Cookie 重新建立会话;并说明该场景与「登录失效通知 → 更新 Cookie」场景的区别。保留当前保守行为（不回退初始 Cookie），本轮不改代码行为。

## Key interfaces

- 仓库根 compose 文件:`docker-compose.yml` → `compose.yaml.example`（模板）+ 被忽略的本地 `compose.yaml`
- `.gitignore`
- README 的「部署（Docker Compose 示例）」「首次 Cookie 配置」「/data 卷要求」「镜像」章节
- `.env.example`（如需随新模型调整说明）

## Acceptance criteria

- [ ] 仓库提交 `compose.yaml.example`;用户复制为 `compose.yaml` 后 `docker compose config` 解析通过
- [ ] `compose.yaml` 与 `data/` 在 `.gitignore` 中;`git status` 不意外跟踪本地部署文件与新落盘的 `data/` 内容
- [ ] 默认 compose 以 named volume 挂载 `/data`（非 `./data` bind mount）
- [ ] README 与 compose 默认镜像路径为 `ghcr.io/UsirukEsikam/weread-cron`，无「替换为你的用户名」指令;fork 发布为单独小节
- [ ] README 含 `books` 的 compose 调用示例与完整镜像更新流程（pull + recreate）
- [ ] README 说明损坏 persisted Login Session 的恢复步骤（删除损坏文件/恢复备份后重启），并与登录失效场景区分
- [ ] `docker-compose.yml` 不再作为活动部署文件（删除或明确废弃）

## Out of scope

- 修改 `session.New` 对损坏会话的回退行为（保守失败是 V1 决定，本票只要求文档化）
- README/compose/.gitignore 之外的文档重构
- release.yml 的镜像标签语义（那是 ticket 21 的范围）
- 为已有部署者迁移 `./data` 数据到 named volume 的工具（README 说明即可，不做工具）

## 验证记录

本轮 triage 已对当前仓库逐项核验，全部属实:

- F1: 仓库根 `docker-compose.yml` 已提交且被 README 引用;无 `compose.yaml.example`。
- F2: compose `volumes: ./data:/data`;README「/data 卷要求」描述 bind mount 模型。
- F3: `.gitignore` 仅有 `.env` 等规则，无 `/compose.yaml`、无 `/data/`。
- F4: README（`ghcr.io/<你的 GitHub 用户名>/weread-cron`）、compose（`ghcr.io/OWNER/weread-cron:latest`）、release.yml（`ghcr.io/${{ github.repository }}`）三处均为 OWNER 替换模型。
- F6: README 无 `books` compose 示例、无镜像更新流程。
- F8: `internal/session/session.go` `New()` 在 `HasLoginSession()==true` 时 `LoadJar` 失败或空 jar 直接返回错误，不回退初始 Cookie;`internal/scheduler/scheduler_test.go` `TestDaemonStartupFailsOnEmptySessionFile` 断言 daemon 启动失败;README「更新 Cookie 后重启自愈」仅限「登录已失效」通知场景。