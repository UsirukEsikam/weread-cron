> *This was generated by AI during triage.*

# 23: Controlled real-account protocol validation

**Source:** findings/03（V1–V11）· .scratch/weread-cron-v1/findings/03-protocol-validation-gaps.md（Status: tracked）
**Category:** enhancement（协议验证工作项，需人工真机执行）
**Blocked by:** None
**Status:** ready-for-human

**What to build:** 用真实微信读书账号 + 浏览器 Cookie 执行一轮受控协议验证，对 `docs/protocol-validation-checklist.md` 的验证项逐项实测并记录证据。checklist 是协议验证事实与结果的主载体（各行「实机验证结果」列 + 「验证记录」章节），本票只代表「执行这一轮验证」这一工作项，不复制独立验证状态。

覆盖范围:

1. checklist #1–#10 现有全部未实测项:Shelf 端点、登录失效判别特征、progress=100% 计费、renewal Cookie 集与 Set-Cookie 属性、`reader.token` 生命周期、`s` 签名边界、Context 刷新后是否必须 re-enter、Context 生命周期 / 15 分钟 TTL、success boundary（`succ` / `synckey` 组合）、enter 必要性。
2. 新增 #11:`rt` interval 语义的真实计时效果——服务端按真实墙钟间隔计时还是折叠/合并;大间隔是否被拒收;间隔异常后重建 Reading Session（重新 enter）是否被接受。对照 ADR-0004 的设计选择。
3. 新增 #12:完全未阅读过的 Shelf 书的 Reader Context 结构——`__INITIAL_STATE__` 是否含当前章节与位置、最小可建 Context 的状态。
4. findings/03 V8/V9 细节已并入 checklist #4（不单独列项）:失效 renewal 是否返回有意义的 Set-Cookie、发送侧简化 Cookie 匹配（不分域/路径）是否充分。

明确不验证:

- findings/03 V11（异常间隔阈值）:内部经验默认值（~90s），不是服务端协议阈值，不作为验证项。真实使用中若观察到问题，再据证据调整。

**Why not delegated（ready-for-human）:** 需要真实账号与个人浏览器 Cookie 的人工会话，含对照参考项目（`weread.koplugin` / `wxread`）当前行为与抓包取证;AFK agent 无凭据，无法闭环。

## Current behavior

- checklist #1–#10 全部为「当前假设（待验证）」;实现票 01–10 的 resolved 只表示基于当前协议假设的实现已完成，**不代表**对应协议事实已获真实账号验证。
- findings/03 的 V1–V11 尚无任何真实账号证据;V4（rt 语义）仅有 ADR-0004 的「设计选择，非协议事实」标注;V10（未读过的书）此前无任何跟踪记录。
- 本轮 triage 已在 checklist 新增 #11/#12、扩充 #4，并加入「实机验证结果」列与「验证记录」章节。

## Desired behavior

1. 一次受控验证会话，按 checklist「验证方式」逐项记录;结果写入各行「实机验证结果」列。
2. 完整会话日志（日期、账号环境、请求/响应摘要、结论）追加到 checklist「验证记录」章节。
3. 验证结论与当前假设不一致 → 在受影响 ticket（02/04/05/06/09/22）的 Comments 发起讨论，由维护者裁决;不静默替换接口/字段/签名方案。
4. 全部证据齐备后，由维护者确认将 findings/03 从 tracked 更新为 resolved;本票完成前一律不标 resolved。

## Key interfaces

- `docs/protocol-validation-checklist.md`（#1–#12 及验证记录章节）
- `.scratch/weread-cron-v1/findings/03-protocol-validation-gaps.md`（V1–V11 缺口清单）
- 受影响实现票:02、04、05、06、09、22
- ADR-0004（rt 语义设计选择，待实测裁决）
- 参考项目:`weread.koplugin`、`wxread`（对照行为）

## Acceptance criteria

- [ ] checklist #1–#12 每行「实机验证结果」有结论（含证据日期与账号环境）
- [ ] checklist「验证记录」章节含完整会话日志
- [ ] 实测与假设不一致的项，已在对应 ticket 的 Comments 发起讨论并有维护者裁决
- [ ] findings/03 状态由维护者确认更新（tracked → resolved，或保持 tracked 并注明剩余缺口）

## Out of scope

- 本票不修改任何实现代码;代码调整走受影响 ticket 讨论后的变更
- findings/03 V11 不作为验证项（内部经验默认值，见上）
- 不复制独立验证状态;验证事实与结果只写入 checklist

## 验证记录

本票为 triage 建票时生成的工作项，尚无实证记录。triage 已完成的映射核对:

- findings/03 → checklist:V1↔#1、V2↔#3、V3↔#2、V5↔#7、V6↔#9、V7↔#5、V8/V9↔#4（本轮扩项）、V4↔#11（新增）、V10↔#12（新增）;V11 按维护者裁决不列项。
- checklist #6（`s` 签名边界）、#10（enter 必要性）在 findings/03 无对应 V-item，继续保留为验证项。
- `.out-of-scope/` 无与本票相关的既有条目。