> *This was generated by AI during triage.*

# 22: Report-success reference comment

**Finding:** F7 · .scratch/weread-cron-v1/findings/02-deployment-and-operations.md
**Category:** bug（低优先，注释失准）
**Blocked by:** None
**Status:** ready-for-agent

**What to build:** 修正 `internal/weread/protocol.go` 中 `IsAccepted` 的注释与证据描述:保持当前 OR 行为（succ 成功 或 synckey 存在即接受），不再写成「两参考项目共识」。准确表述为——该语义与 `weread.koplugin` 一致;`wxread` 更严格（succ 与 synckey 需同时存在才推进进度）;真实服务端 success boundary 仍属 protocol validation gap（checklist #9），本轮不改变 `IsAccepted` 行为。

## Current behavior

`internal/weread/protocol.go`（约 264–266 行）`IsAccepted` 注释:

> 两参考项目共识:succ==1 或 synckey 存在即视为接受（checklist #9 的边界语义未经实测，本函数不写死服务器语义断言，仅实现参考项目共识）。

实际参考源码行为:

- `weread.koplugin`:接受 `succ` 成功 **或** `synckey` 存在（OR）——与当前实现一致;
- `wxread`（findmover/wxread `main.py`）:`succ` 与 `synckey` **同时存在**才推进进度计数（AND，更严格）。

「两参考项目共识」表述失准。

## Desired behavior

- 保持 `IsAccepted` / `IsSucc` 行为不变（OR）
- 注释准确分述两个参考项目:本实现与 `weread.koplugin` 一致;`wxread` 更严格（succ 与 synckey 均需存在）;两者并不构成「共识」
- 保留「真实服务端边界未经实测 / protocol validation gap（checklist #9）」的标注

## Key interfaces

- `internal/weread/protocol.go`:`IsAccepted` 的注释（及可同步的相邻文档注释）
- `docs/protocol-validation-checklist.md` #9 的表述（如与注释不一致需同步）

## Acceptance criteria

- [ ] `IsAccepted` 注释不再声称两参考项目共识;准确分述 koplugin（OR）与 wxread（AND，更严格）
- [ ] 行为不变:相关测试（`TestIsAccepted` 等）全绿
- [ ] 保留「边界未经实测 / protocol validation gap」标注（checklist #9）

## Out of scope

- 修改 `IsAccepted` / `IsSucc` 行为（保持 OR）
- 协议验证清单本身的边界确认（需真实账号，见 findings/03）

## 验证记录

- 本地注释确认于 `internal/weread/protocol.go`（约 264–266 行），确为「两参考项目共识」表述。
- 一手来源（findmover/wxread `main.py`）:

  ```python
  if 'succ' in resData:
      if 'synckey' in resData:
          lastTime = thisTime
          index += 1
          ...
  ```

  即 `succ` 与 `synckey` 同时存在才计数（AND，更严格）。
- koplugin 侧:本地注释与 checklist #9 记载 koplugin 以 `succ==1` 或 `synckey` 存在判定接受（OR）。
- 判断:注释不能继续写成「两参考项目共识」;真实服务端 success boundary 保持为 protocol validation gap，本轮只修注释与证据描述。