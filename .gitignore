# build artifacts
/weread-cron
/weread-cron.exe
/dist/
/coverage.out

# OS noise
.DS_Store

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace file
go.work
go.work.sum

# env file
.env

# local deployment state（compose.yaml.example 复制后的本地部署文件；data/ 为
# 运行时状态，内含完整持久化 Cookie 集合，不得入库）
/compose.yaml
/data/
